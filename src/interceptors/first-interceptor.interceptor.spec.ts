import { FirstInterceptorInterceptor } from './first-interceptor.interceptor';

describe('FirstInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new FirstInterceptorInterceptor()).toBeDefined();
  });
});
